{
  "name": "@clock/web",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && vite build --outDir dist --emptyOutDir",
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clock/clock": "0.0.0"
  },
  "devDependencies": {
    "vite": "^7.3.6"
  }
}
