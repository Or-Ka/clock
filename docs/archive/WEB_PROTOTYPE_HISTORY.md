# Web Prototype History

Status: superseded.

The project previously used prototype-era web screens while the library and product model were being explored.

Superseded locations:

- `apps/demo`
- `@clock/demo`
- `src/dual-ring-events`
- static clock screen
- live clock screen
- SVG spike screen

Current official application:

- `apps/web`
- `@clock/web`
- `Analog Event Clock`

Historical code that still has reference value is kept under `archive/legacy-app-screens` and is not part of the active application build.
